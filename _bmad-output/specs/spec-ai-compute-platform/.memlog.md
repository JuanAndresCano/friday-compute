---
topic: Spec Task 2 — Extended High-Level Spec & Architecture Spine for the University AI Compute Management Platform
goal: Elevate Spec-1 into a handoff-ready SPEC + Architecture Spine that passes the Handoff Test
student: Juan Andrés Cano Ramírez (A00399560)
updated: 2026-08-31T16:39
---

- (event) Session opened. Inputs loaded: Spec-1 (Cano A00399560), original project brief, Workshop 2 peer review by J.E. Ruiz (A00399562), Spec-2 task statement + 100-pt rubric.
- (direction by user) Execution mode chosen: guided sequential — invoke bmad-product-brief -> bmad-spec -> bmad-architecture in order; adversarial self-review + human checkpoint after each phase.
- (decision by user) The undefined 'equivalent-or-better hardware' comparison (peer-review open item) will get a concrete TESTABLE definition in SPEC.md, not a deferral. Rationale: CAP for reservation-conflict substitution depends on it; leaving it undefined re-imports the Clarity/decomposition-readiness gap Ruiz flagged.
- (note) Peer-review findings to remediate (Ruiz): Atomicity WEAK ('Create reservation' bundles admission + window-start preemption); Traceability WEAK (7 unlabeled capability bullets, no CAP IDs, no map to Contract actions); Clarity MEDIUM (queue 'estimated start time' compute/accuracy undefined); Decomposition-readiness MEDIUM (same root cause as Atomicity). Strong dims to preserve: Scope control, Verifiability, Consistency, Impl. independence.
- (event) PHASE 1 (Product Brief) drafted at design-artifacts/A-Product-Brief/project-brief.md via fast/draft path.
- (decision) Brief carries 7 strategic KPIs (KPI-1..7) mapped to the task rubric's success-metric hints (zero class disruption, +/-5% quota accuracy, 100% named denials, queue-vs-reject correctness, utilization visibility, conflict resolution, audit completeness). Binary acceptance tests deferred to SPEC.md by design — keeps the brief strategic and avoids WHAT/HOW bleed.
- (decision) 5 proto-personas fixed to the task's named stakeholder set (admin, instructor, student, operator, researcher), each tied to a role tier consistent with Spec-1's coarse role model. Names are illustrative; deep profiles pushed to companion-files/persona-archetypes.md.
- (assumption) Differentiation framed as institutional-infra value (governance = enforceable guarantee), NOT a competitive moat. Self-review rejected adding a moat narrative as dishonest for a non-commercial internal platform.
- (question) 3 strategic open questions carried forward: (a) single vs per-course quota policy for iteration 1; (b) is full-cohort concurrent surge a first-class design target (Spec-1 tagged Risky); (c) source-of-truth ownership for academic-context membership + class schedule, and whether a fallback authoring path is needed.
- (event) Adversarial self-review of Brief: no implementation leakage; KPIs trace to rubric; personas consistent with Spec-1 roles; no fabricated moat. Minor: KPI-2 (+/-5% / 100 streams) is rubric-supplied and needs a measurement-window definition in SPEC to be truly binary. Awaiting human checkpoint.
- (note by user) CORRECTION to earlier log framing: 'friday-compute' is the repo name the student chose, NOT an agent-invented codename. Do not treat it as a creative addition requiring confirmation.
- (event by user) Checkpoint 1 REDIRECT: user rejected KPI-1 and KPI-2 as near-verbatim copies of the Spec-2 task statement's *illustrative* Success Signal examples ('100 concurrent inference streams within 5%', '14-day simulated academic cycle'). Ruled unearned precision = the invented-constraint anti-pattern already corrected in Spec-1's numeric thresholds. Demanded a full provenance scan of every number/label in the brief.
- (decision) Provenance scan result: KPI-3/4/6/7 = verbatim from Spec-1 Success Signal (real). KPI-1 zero-disruption = Spec-1 (real) but its '14-day' window = task example (tagged). KPI-2 '100 streams' + 'plus/minus 5%' = task example only; Spec-1 commits only to 'moderate concurrent load' (both tagged). KPI-5 staleness bound = intentional placeholder, deferred to SPEC. Hardware figures (24GB / 2x48GB, 'many' nodes) = project brief + Spec-1 (real). Persona names = fictional placeholders.
- (change) Brief S5 rewritten: each KPI now = a Spec-1-derived PROPERTY + a PROVISIONAL number, with a Provenance column. Task-example numbers kept but tagged [ILLUSTRATIVE DEFAULT ... pending real capacity data], mirroring Spec-1's tunable-default pattern. Added explicit notes that 'fair quota distribution' and 'utilization rate' targets are NOT given a fabricated number. Persona intro now states names are fictional; 'Dr. Elena Ruiz' -> 'Dr. Elena Navarro' to remove collision with peer reviewer J.E. Ruiz.
- (direction) Standing rule for all downstream phases: any numeric threshold not traceable to Spec-1, the project brief, or a stated real-world datum must be tagged [ILLUSTRATIVE DEFAULT] with its provenance and a replace-with condition. No unearned precision.
- (event by user) Checkpoint 1 APPROVED. Carry-forward directive: Brief S5 stays a short property-level summary; the full KPI provenance + replace-with formulas + binary acceptance tests live ONLY in SPEC.md Success Signal, no near-verbatim duplicate table across the two docs.
- (change) Brief S5 trimmed to property + provisional-number columns; Provenance column and (node count x sessions) formula removed from the brief, to be authored in SPEC.md instead.
- (event) PHASE 2 (SPEC.md) started via bmad-spec, fresh phase context.
- (capability) SPEC.md drafted with 12 capabilities: CAP-1 admission+named-rejection taxonomy; CAP-2 multi-tier quota & token metering (pre-emptive reserve + reconcile); CAP-3 role tiers + academic-context scoping; CAP-4 heterogeneous placement; CAP-5 reservation ADMISSION; CAP-6 reservation WINDOW ENFORCEMENT & preemption; CAP-7 equivalent-or-better substitute (now a testable predicate); CAP-8 distributed/high-VRAM training lifecycle; CAP-9 node health-state query; CAP-10 health-driven eviction & re-queue; CAP-11 usage reporting; CAP-12 audit trail.
- (decision) Workshop-2 Atomicity remediation: Spec-1's single 'Create reservation' action split into CAP-5 (admission: GRANTED/SUBSTITUTE_OFFERED/PENDING_ADMIN_REVIEW) and CAP-6 (window-start preemption: signal + grace + forced-terminate + PREEMPTED!=FAILED). Different trigger, different failure mode -> separately testable.
- (decision) Workshop-2 Traceability remediation: every capability now CAP-n; constraints numbered C-1..C-11; non-goals NG-1..NG-11; assumptions A-1..A-10; open questions OQ-1..OQ-5 -- stable anchors for ADRs/tests.
- (decision) Workshop-2 open item ('equivalent-or-better' undefined) resolved per user directive as CAP-7: 4-part iff predicate (per-GPU VRAM >=, GPU count >=, arch class compatible, free for whole window) + deterministic ranking (min VRAM, then min GPU count, then node id asc). No qualifier -> PENDING_ADMIN_REVIEW.
- (decision) Workshop-2 Clarity remediation: CAP-1 QUEUED outcome now carries an EXACT queue position (binary-verifiable vs dequeue order) plus an explicitly ADVISORY estimated start time with a stated computation basis (projected release times of jobs ahead). Removes Spec-1's undefined-estimate gap without over-committing to an accuracy number.
- (constraint) C-1..C-11 recorded. Load-bearing new ones vs Spec-1: C-2 whole-GPU allocation granularity (no MIG in iter 1); C-3 CAP-8 lifecycle must map to what Kubeflow Training Operator observably reports; C-10 academic priority is absolute (no tier overrides an active reservation); C-11 clock sync coarse-grained only.
- (assumption) A-1..A-10 recorded; [Risky]: A-1 exact token counts, A-2 moderate-load-only, A-3 accurate class-schedule source, A-4 Kubeflow/device-plugin version support, A-5 research jobs can checkpoint in grace period. [Safe]: A-6 GPUs schedulable, A-7 coarse clock sync, A-8 whole-GPU granularity, A-9 contexts exist upstream, A-10 single institution-wide quota policy for iter 1.
- (event) SPEC self-validate Pass 1 (Coherence, Spec Law 1-6,8): PASS. Fixes applied during sweep: (a) removed self-invented [ILLUSTRATIVE DEFAULT: 30s/60s] staleness numbers from CAP-9/CAP-11 and KPI-5 -- replaced with 'tunable SLO, value at OQ-5, no default invented', to stay consistent with Spec-1's no-number threshold style and the standing no-unearned-precision rule.
- (event) SPEC self-validate Pass 2 (Preservation): walked every Spec-1 claim. One gap found and fixed: Spec-1's 'queue position + estimated start time' was missing from CAP-1 -- added (also serves the Clarity remediation). All other Spec-1 claims (entities, 5 rejection reasons, quota timing, reservation outcomes, preemption, node states, usage query, audit fields, constraints, non-goals, 5 assumptions, success signal) map to a CAP-n / C-n / NG-n / A-n. No load-bearing claim dropped.
- (question) OQ-1..OQ-5 open: per-course quota overrides; full-cohort surge as design target; source-of-truth ownership + fallback authoring for membership/schedule; CAP-7 ranking = smallest-sufficient vs load-balancing; concrete values + policy owner for all [ILLUSTRATIVE DEFAULT] thresholds.
- (event) PHASE 2 (SPEC.md) draft complete, awaiting human checkpoint 2.
- (event by user) Checkpoint 2 REDIRECT: 5 changes consolidated from stakeholder input (course professor acting as project stakeholder).
- (decision by user) CAP-7 generalized: reframed from 'reservation substitute' to 'Equivalent-or-Better Node Resolution (shared mechanism)' invoked by CAP-5, CAP-6, CAP-10. Predicate unchanged (per-GPU VRAM, GPU count, arch class, availability over span S). Returns NO_QUALIFYING_NODE; each caller defines its own fallback.
- (decision by user) CAP-10 rewritten: node failure now RELOCATES via CAP-7 (workload is admitted against capacity, not a named machine -- confirmed vs original brief 'reservation of resources or services'). Relocated workload keeps state + queue standing, not marked FAILED. Only if NO_QUALIFYING_NODE -> enters queue at the FRONT of the entire queue, ahead of all pending submissions of any type.
- (decision by user) CAP-6 rewritten to unify with CAP-10: at reservation window start, try relocation first (same CAP-7/CAP-10 procedure by reference). Only NO_QUALIFYING_NODE triggers the termination-signal + grace + forced-terminate fallback, and ONLY that fallback path records terminal state PREEMPTED. Relocated workloads are never PREEMPTED. Resolves the PREEMPTED-resumability question: PREEMPTED is terminal by construction (only reached after relocation already failed).
- (decision by user) NG-8 reworded: CAP-7's equivalent-or-better predicate AND its deterministic tie-break are a FIXED part of the contract, not deferred to architecture. Only *further* optimisation layered on top (bin-packing, anti-fragmentation, cost/energy, predictive placement) stays an architecture concern. Removes the NG-8<->CAP-7 contradiction.
- (decision by user) CAP-2 gap closed: if reserving the estimated cap would take either the personal or context balance below zero, the workload is REJECTED before execution with the matching CAP-1 reason (exhausted-personal-quota / exhausted-context-quota) -- never admitted-then-truncated-to-fit.
- (constraint by user) C-12 added (stakeholder-confirmed): assignable fleet is a physically uncontrolled student lab (<40 machines), hardware can vanish without warning; control-plane must not depend on it and node loss is a routine handled event.
- (assumption by user) A-11 [Safe] added: control-plane runs on independent server infra isolated from the student lab, provisioning/availability managed outside this platform (NG-3, C-12) -- confirmed by professor. A-12 [Risky] added: node failures assumed infrequent enough that CAP-10 front-of-queue relocation does not starve fresh submissions; fairness risk unresolved at design stage -> OQ-6. C-11 cross-ref corrected (A-4 -> A-7). C-1 kept verbatim, no exact fleet count introduced.
- (event) SPEC re-validated after Checkpoint-2 redirect. Coherence fixes: identifier ranges updated (C-1..C-12, A-1..A-12, OQ-1..OQ-6); CAP-8 clarified that a CAP-6/CAP-10 relocation is NOT a lifecycle transition (job stays ACTIVE) and PREEMPTED/COMPLETED/FAILED are terminal. Cross-refs CAP-5/6/7/10 checked consistent; CAP-1 rejection-reason tokens match CAP-2. No Spec Law violation.
- (question) PENDING FOR USER: whether to add C-13 'workloads bind to a capacity class, not a named physical node' as an explicit structural invariant. The principle is currently embedded in CAP-10 intent + CAP-7 framing; promoting it to a numbered constraint would give the architecture phase a firmer anchor. Not added unprompted — user was precise about the change list.
- (decision by user) OQ-1 RESOLVED: single institution-wide quota policy for iteration 1 (per-course overrides deferred); decision of record = A-10. OQ-4 RESOLVED: CAP-7 smallest-sufficient-node ranking; decision of record = CAP-7 tie-break. Both kept in the OQ list with RESOLVED status as an audit trail, not deleted.
- (constraint by user) C-13 ADDED: workloads/reservations bind to a resource requirement (GPU count, min per-GPU VRAM, arch class, duration/window), never to a physical node identity. Node identity = where the requirement is currently met; changes under CAP-6/CAP-10 without changing workload identity. Architecture Spine state-mutation rules MUST NOT persist a durable workload-to-node binding as source of truth. Shared invariant behind CAP-4/5/6/7/10.
- (event by user) Checkpoint 2 APPROVED. SPEC.md final for this task: 12 capabilities, C-1..C-13, NG-1..NG-11, A-1..A-12, OQ-1..OQ-6 (OQ-1/OQ-4 resolved). Proceed to Phase 3.
- (event) PHASE 3 (ARCHITECTURE-SPINE.md) started via bmad-architecture. Inherits SPEC as the spine's home; C-13 is the load-bearing state-mutation constraint to honour.
- (version) Web-verified (Aug 2026): Kubernetes v1.37 latest stable (v1.36 still supported); Kubeflow TRAINER v2 (v2.2) is now the standard, single TrainJob CRD, replacing framework-specific v1 Training Operator (v1 maintained only on release-1.9); sigs.k8s.io/controller-runtime tracks k8s minor; NVIDIA GPU Operator 24.9 / k8s-device-plugin (whole-GPU strategy, MIG/time-slicing available but not used per A-8).
- (question) UPSTREAM DRIFT FOUND: SPEC C-3 says 'Kubeflow Training Operator's job types' but v1 Training Operator is deprecated as of 2026. Architecture binds to Trainer v2 TrainJob (AD-12) and flags C-3 for an upstream SPEC wording update.
- (decision) PARADIGM (AD-1): Layered Control Plane with a Hexagonal governance core + a separate event-driven telemetry plane. Binds: all. Prevents: K8s/Kubeflow/serving SDKs leaking into quota/reservation domain logic; telemetry volume degrading admission latency. Rule: governance domain imports zero infra SDKs; external systems reached only through ports; telemetry never on the synchronous governance path.
- (decision) AD-2 desired-state reconciliation: Reservation Coordinator + Placement/Scheduler Bridge are level-triggered idempotent controllers reconciling stored desired state toward observed cluster state; no imperative one-shot scheduling (must self-heal after restart / missed event).
- (decision) AD-3 single source of truth: the Governance Core transactional store (Postgres) is authoritative for all governance state; cluster state (pods, TrainJobs, node conditions) is observed/derived, never authoritative for a decision. Every governance decision reads the store, not a cluster cache.
- (decision) AD-4 [ADOPTED from SPEC C-13]: workload/reservation binds to resource_requirement, never node identity. Schema rule: no node id may be a required key/FK of a workload or reservation; current_placement_ref is a nullable, advisory, last-observed pointer updated outside any domain transaction (relocation/preemption change it freely).
- (decision) AD-5 quota concurrency: reserve + reconcile are the ONLY writers of a balance; both personal and context balances updated in one serializable transaction with rows locked in ascending-id order (deadlock-free); balance < 0 forbidden by a DB CHECK constraint; over-reserve rejected pre-execution (CAP-2).
- (decision) AD-6 reservation non-overlap enforced in the datastore: reservation intervals per hardware class carry a Postgres EXCLUDE USING gist non-overlap constraint; under concurrency the losing overlapping request deterministically falls through to CAP-7, never both GRANTED.
- (decision) AD-7 equivalent-or-better resolver is a pure deterministic domain function resolve(requirement, span, fleetSnapshot) -> Node | NO_QUALIFYING_NODE, zero I/O, fixed tie-break (min per-GPU VRAM, then min GPU count, then node id asc). One implementation, three callers (CAP-5/6/10).
- (decision) AD-8 relocate-first / preempt-fallback is one shared procedure relocateOrElse(workload, fallback): CAP-7 hit -> move & keep lifecycle state + queue standing; NO_QUALIFYING_NODE -> caller fallback (CAP-10: front-of-queue; CAP-6: grace-then-terminate + terminal PREEMPTED). Binds CAP-6+CAP-10 so they cannot diverge.
- (decision) AD-9 plane separation: synchronous governance plane (CAP-1..8,12) vs asynchronous telemetry plane (CAP-9,10,11). Telemetry (node health, GPU/DCGM metrics, serving-layer token counters) flows only over the event bus into the Telemetry Ingestion Engine; governance API never blocks on telemetry; node.failing reaches controllers as an event that triggers AD-8.
- (decision) AD-10 transactional audit: every governed mutation writes its CAP-12 audit row in the SAME transaction as the mutation, plus a transactional-outbox row; a relay publishes the outbox to the bus at-least-once. Prevents a mutation committing without its audit entry (or vice-versa) on crash. Serves C-8.
- (decision) AD-11 [ASSUMPTION] desired state modelled as CRDs (Reservation, GovernedWorkload) reconciled by controller-runtime controllers; GovernedWorkload controller owns the Kubeflow TrainJob for distributed jobs. Chosen over Postgres-only desired state + K8s-as-dumb-actuator for kubectl-observability/debuggability. Revisit if CRD churn becomes an operational burden.
- (decision) AD-12 distributed training binds to Kubeflow Trainer v2 TrainJob (single CRD), NOT v1 framework CRDs (PyTorchJob/TFJob/MPIJob, deprecated). Rejected: Training Operator v1 (EOL track); Volcano/raw gang-scheduling (reinvents Trainer v2). Consequence: SPEC C-3 wording update needed upstream.
- (decision) STACK SEED (fast-path, [ASSUMPTION] where noted): Go 1.24+ (paved path for k8s control planes; rejected Python/FastAPI - weaker long-running-controller story, Rust - team velocity); sigs.k8s.io/controller-runtime + kubebuilder; Kubernetes 1.37 target (>=1.36); NVIDIA GPU Operator 24.9 whole-GPU; Kubeflow Trainer v2.2; PostgreSQL 17 (serializable tx, CHECK, EXCLUDE gist for reservation non-overlap); [ASSUMPTION] NATS 2.11 + JetStream as event bus (lighter than Kafka for <40-node on-prem); [ASSUMPTION] Prometheus + DCGM exporter, TSDB choice (plain Prometheus vs VictoriaMetrics) deferred; gRPC internal / REST+JSON edge; external OIDC (adapter only, NG-9).
- (decision) COMPONENT BOUNDARIES: (1) API Gateway/Auth Proxy - no domain state; (2) Governance Core (hexagonal domain: CAP-1/2/3/4/7); (3) Reservation Coordinator (CAP-5/6 controller); (4) Placement/Scheduler Bridge (CAP-8/10, emits pod specs + TrainJobs, runs AD-8); (5) Kubeflow/K8s Adapter (only component touching cluster API/device plugins/Trainer v2); (6) Telemetry Ingestion Engine (CAP-9, emits node.failing); (7) Usage & Audit Service (CAP-11/12, append-only); (8) Event Bus. Edge REST, internal gRPC, telemetry+domain events on the bus, K8s watch/informer for actual state.
- (decision) STATE-MUTATION INVARIANTS also fixed: workload lifecycle_state (CAP-8) is SoT, transitions guarded by an explicit state-machine table, PREEMPTED/COMPLETED/FAILED terminal, relocation is NOT a transition; every controller reconcile idempotent + level-triggered; CAP-1 queue position derived from a per-hardware-class ordered admission log; CAP-7 reads a point-in-time fleet snapshot (no mid-resolution mutation).
- (decision) CONSISTENCY CONVENTIONS: opaque string ids (per Spec-1, no internal structure assumed); RFC3339 UTC timestamps; CAP-1 error envelope = {named_reason enum, quota_snapshot}; events named <entity>.<past-tense> (reservation.granted, workload.preempted, node.failing, quota.reconciled); CRD group compute.university.edu/v1alpha1; Go hexagonal layout domain/ app/ adapters/ controllers/ cmd/.
- (decision) DEFERRED: queue-ordering heuristic beyond CAP-1 position + CAP-7 tie-break (NG-8); control-plane HA topology / leader election ([ASSUMPTION] deferrable - A-11 says infra managed externally); multi-cluster/multi-lab federation (out for <40-node single lab); concrete [ILLUSTRATIVE DEFAULT] values (OQ-5); CAP-10 fairness bound (OQ-6); per-course quota policy engine (OQ-1 resolved: not now); backup/DR, secrets, network policy (operational envelope, external infra team per NG-3/A-11 - named so it doesn't fall through); full API payload schemas (story-level).
- (event) PHASE 3: memlog capture complete (AD-1..AD-12 + stack + boundaries + invariants + conventions + deferred). Distilling ARCHITECTURE-SPINE.md next.
- (event) ARCHITECTURE-SPINE.md distilled from memlog. Adversarial self-review: (1) ADs enriched to full ADR form (Context/Binds/Prevents/Rule/Status/Consequences[gained vs sacrificed]/Rejected) to meet the 15% ADR rubric; (2) operational dimension closed with a 'Deployment & Environments' block (two failure domains by rule: control plane on A-11 isolated infra, lab machines as worker nodes only, control-plane node selector/taint; node loss = routine event; HA of control-plane host deferred to external team).
- (event) Spine consistency check vs SPEC: C-13->AD-4, C-8->AD-10, C-3->AD-12(+upstream flag), C-2->Stack note, C-7->CAP-3 left open (no RBAC/ABAC pick), C-11->RFC3339 sec-precision, NG-9->OIDC adapter only, NG-8->Deferred, A-11/C-12->Deployment block, OQ-1/2/5/6->Deferred. 12 ADs, 8 components, dep-direction + ERD mermaid inline. No capability added, no constraint relaxed.
- (event) PHASE 3 (ARCHITECTURE-SPINE.md) draft complete, awaiting human checkpoint 3.
- (decision by user) AD-6 CORRECTNESS FIX: per-class pairwise EXCLUDE non-overlap was wrong for the 24GB class (many interchangeable nodes per SPEC C-1) - it would make 20 nodes act as 1 slot. Rewritten: each class has unit_count; a granted reservation holds an ABSTRACT capacity slot (class_id, slot_no in 1..unit_count); EXCLUDE USING gist (class_id=, slot_no=, during &&) so at most unit_count reservations overlap in time; lowest free slot assigned deterministically. slot_no is a capacity index, NOT a physical node - placement still picks the node at window start (preserves AD-4/C-13). Rejected binding to a physical node at grant time (would violate C-13).
- (decision) AD-13 ADDED (clarifies CAP-2 <-> AD-9 boundary): quota ADMISSION is synchronous and serving-layer-free (reads/writes only stored balances); ENFORCEMENT is local truncation in the serving-layer adapter against the cap it was handed; RECONCILIATION is asynchronous - serving layer emits workload.tokens.finalized on the telemetry plane, Governance Core runs the reconcile tx off that event. No dedicated synchronous serving-layer path. Trade-off: balance briefly optimistic between completion and reconcile event; lost event repaired by a periodic sweep.
- (change by user) SPEC.md C-3 updated (approved): 'Kubeflow Training Operator's job types' -> 'Kubeflow Trainer v2 single TrainJob CR', with a Spec-2-update note pointing to AD-12. A-4 also updated to name Trainer v2 / TrainJob instead of the v1 Training Operator.
- (event by user) AD-11 (CRD-based desired state) APPROVED as-is by user; [ASSUMPTION] tag retained as informational but decision is confirmed.
- (note) OQ-1 Deferred-list row reviewed: text is 'Per-course quota-policy engine | SPEC OQ-1 resolved: single institution-wide policy for iteration 1.' - it does NOT claim OQ-1 is open; it is a contextual pointer to the resolution. Left as-is per user instruction.
- (event) Spine consistency re-swept after AD-6 fix + AD-13 add: CAP-2 map row -> +AD-13; conventions events list -> +workload.tokens.finalized; AD-9 rule -> pointer to AD-13. 13 ADs total.
- (decision by user) AD-13 Consequences extended: explicit note that within the optimistic-balance window a second back-to-back workload from the same student/context is checked against a not-yet-reconciled balance, so transient over-consumption across two consecutive workloads is possible (AD-5 non-negativity still holds per single transaction). ACCEPTED AS-IS - bounded by one estimate-vs-actual gap, self-corrects at next reconcile, within SPEC A-2 moderate-load. No per-actor in-flight cap added this late; revisit only if A-2 lifted or reconcile latency large.
- (event by user) Checkpoint 3 APPROVED. ARCHITECTURE-SPINE.md final for this task: paradigm + 8 components + AD-1..AD-13 + conventions + stack + cap-map + structural seed + deployment + deferred. Proceeding to Phase 4.
- (event) PHASE 4 started: companion-files/glossary.md, companion-files/persona-archetypes.md, companion-files/diagrams/system-context.mmd, companion-files/diagrams/state-transitions.mmd.
- (event) PHASE 4 drafted: glossary.md (40+ canonical terms, all task-example terms covered incl. 'Preemptible Priority Tier' explicitly marked OUT of the model per C-10; every term cross-linked to CAP/C/AD ids); persona-archetypes.md (5 archetypes = brief personas x SPEC role tiers, each with motivation/friction/what-changes/permission-boundary/tensions + a cross-persona tension map); diagrams/system-context.mmd (flowchart C4-L2 container view: 8 components, upstream systems, sync governance plane solid vs async telemetry plane dotted); diagrams/state-transitions.mmd (combined stateDiagram-v2: workload REQUESTED->SCHEDULED->ACTIVE->{COMPLETED,FAILED,PREEMPTED} + reservation machine + relocation-is-not-a-transition note).
- (note) Mermaid CLI (mmdc 11.16) present but cannot launch headless Chrome in this sandbox; both .mmd files manually reviewed against Mermaid 11 grammar (flowchart + stateDiagram-v2). They render on GitHub / any standard Mermaid renderer.
- (event) Adversarial self-review PHASE 4: glossary + personas consistent with SPEC (role tiers, named-rejection set, lifecycle states, PREEMPTED!=FAILED, capacity slot/unit_count from AD-6, C-13, C-10, AD-8). Persona name 'Dr. Elena Navarro' matches the Phase-1 rename (was Ruiz). No new claims introduced beyond SPEC/spine. Awaiting checkpoint 4.
- (event) Mermaid RENDER-VERIFIED with mmdc 11.16 + puppeteer chrome 152 (--no-sandbox). system-context.mmd -> valid 74765-byte flowchart SVG (48 node spans, 32 edges, no error text). state-transitions.mmd initially FAILED (Lexical error: ';' in a stateDiagram transition label on the GRANTED->WINDOW_ACTIVE line); fixed by replacing ';' with ',' / '.' in labels + note + comment; re-render -> valid 84749-byte state SVG (2 composite states, 30 edges, no error text). Both confirmed non-error diagrams.
- (note) Verified 'named-machine vs requirement-binding' tension wording in persona-archetypes.md frames it as human-expectation-vs-system-design on both occurrences (instructor Tensions line + cross-persona map row) and in the operator/researcher variants; every instance states the system side correctly (reservation/workload binds to a requirement, platform places on any equivalent-or-better node). No slippage toward node-as-unit-of-reservation; consistent with C-13/AD-4/AD-6.
- (event by user) Checkpoint 4 APPROVED (proceed to Phase 5 directly; no redirect). Phase 4 companion set final: glossary.md, persona-archetypes.md, diagrams/system-context.mmd, diagrams/state-transitions.mmd (both render-verified with mmdc 11.16).
- (event) PHASE 5 started: peer-review-remediation.md (Deliverable E) authored at repo ROOT.
- (decision) peer-review-remediation.md structure: verdict summary table (8 dims: old rating -> disposition -> anchor) + dimension-by-dimension matrix (Workshop-2 finding / root cause in Spec-1 / action taken with exact CAP|C|NG|AD id) + capability->Spec-1 provenance map (the 1:1 map Ruiz asked for; CAP-5/CAP-6 shown as the split of Spec-1 'Create reservation'; CAP-7 shown as deferral->defined) + explicit preservation check on the 4 Strong dims + open-items list. Sourced from the actual Workshop-2 docx (re-extracted): Clarity Medium, Scope Strong, Verifiability Strong, Atomicity Weak, Consistency Strong, Impl-independence Strong, Traceability Weak, Decomposition-readiness Medium.
- (event) Adversarial self-review PHASE 5: (1) all 8 rubric dimensions present, each with a real Workshop-2 quote as the finding; (2) Weak x2 (Atomicity->CAP-5/6 split matching Ruiz's suggested rewrite verbatim; Traceability->CAP/C/NG/A/OQ/AD scheme + provenance table) and Medium x2 (Clarity->CAP-1 advisory-estimate scoping; Decomposition->one-trigger-one-success per CAP + shared CAP-7 + capability->component map) each traced to a concrete id; (3) Strong x4 shown preserved, not just asserted - each with the mechanism that carries it forward; (4) SPEC.md forward-ref 'Capability-to-Spec-1 provenance is in peer-review-remediation.md' now satisfied by the provenance map section; (5) no new claims about the system introduced - doc only points at existing CAP/C/AD ids. Ready for Checkpoint 5.
- (decision by user) Checkpoint 5 review, 3 items resolved before Finalization: (1) CONFIRMED - CAP-5/CAP-6 split and CAP-7 formalization presented as remediation-driven (trace to Workshop-2 Atomicity finding + Open Question), not a separate scope-evolution note. (2) 'strengthened' claims re-adjudicated by explicit Spec-1 comparison: Verifiability STAYS strengthened (Spec-1 verifiability lived only in Success Signal + Contract output shapes; its 7 capabilities had zero acceptance tests; Spec-2 adds a binary success clause + 'Verified by' procedure to all 12 CAPs = genuine new verifiable surface). Consistency DOWNGRADED to preserved (Spec-1 was rated Strong precisely FOR demonstrated cross-document term discipline - its own final-pass check; Spec-2's glossary + conventions table formalize/scale that existing property, do not make the spec more internally consistent). (3) Quote-fidelity fix: every dimension entry now separates 'Quoted (Workshop-2 verdict table)' verbatim text in backticks from 'Reviewer's rationale / suggested fix (paraphrased)'; Traceability quote restored to full parenthetical; Atomicity rationale+rewrite de-italicised and labelled paraphrase. Added a 'Quote fidelity' note to the doc intro.
- (event) Checkpoint 5 APPROVED (with the 3 fixes above). peer-review-remediation.md final. Entering FINALIZATION.
- (event) FINALIZATION: SPEC.md and ARCHITECTURE-SPINE.md frontmatter status draft -> final. AD-12 'Upstream action' marked done (SPEC C-3 + A-4 already carry the Trainer v2 wording + back-pointer). Whole-bundle consistency sweep: all 8 deliverable files present at the task-required paths; admission-outcome set / lifecycle-state set / reservation-outcome set / named-rejection enum identical across SPEC, glossary, state diagram; unit_count + capacity-slot wording identical in glossary and AD-6; PREEMPTED-vs-FAILED and relocation-is-not-a-transition stated identically in SPEC CAP-6/CAP-8, glossary, state-transitions.mmd note, persona archetypes; SPEC forward-ref to peer-review-remediation.md for capability provenance now satisfied. No capability added, no constraint relaxed during finalization.
- (version) Spec Task 2 bundle COMPLETE. Deliverables: A design-artifacts/A-Product-Brief/project-brief.md; B _bmad-output/specs/spec-ai-compute-platform/SPEC.md (12 CAP, C-1..C-13, NG-1..NG-11, A-1..A-12, OQ-1..OQ-6); C .../ARCHITECTURE-SPINE.md (paradigm + 8 components + AD-1..AD-13 + conventions + stack + cap-map + structural seed + deployment + 10 deferred); D .../companion-files/ (glossary ~50 terms, 5 persona archetypes, system-context.mmd, state-transitions.mmd - both mermaid render-verified); E peer-review-remediation.md at repo root (8-dimension matrix, verbatim Ruiz quotes separated from paraphrase, capability->Spec-1 provenance map, Strong-dimension preservation check with per-dimension strengthened/preserved adjudication); F .../.memlog.md (86+ entries, append-only). No application code / migrations / deploy config produced - task terminates at the design/spec boundary per task section 6.3.
- (event by user) FINAL PRE-SUBMISSION PASS (per user close-out message, 4 items): (1) Added 'Response to Ruiz's Open Question (Partner Meeting)' section to peer-review-remediation.md right after the verdict table - answers the equivalent-or-better question (testable now via CAP-7, not deferred; cites memlog entry 10) and states plainly that the anticipated in-person pair meeting did not happen before submission and remediation ran from the written .docx only. (2) project-brief.md frontmatter status draft -> final (missed in the earlier Finalization sweep, which only touched SPEC.md and ARCHITECTURE-SPINE.md). (3) Root README.md was a 2-line stub; rewrote it as a navigable bundle index linking all six deliverables A-F at their exact task-required paths + source inputs + reading order (serves the Handoff-Test 'clean and navigable' criterion). (4) This entry. Bundle is submission-ready.
- (change by user) peer-review-remediation.md: added a consolidated 'Traceability matrix (Workshop 2 -> Spec 2)' section immediately after the verdict summary, matching the task statement Section 5 template exactly (4 columns: Dimension | Issue/Finding from Workshop 2 | Root Cause in Spec 1 | Action Taken & Specific Section/ID in Spec 2), all 8 dimensions, one compressed row each with concrete CAP/C/NG/AD anchors. The existing dimension-by-dimension deep-dive sections are retained below it as the detailed backing (verbatim quotes + full Spec-1 comparison + disposition rationale). Done for exact rubric-template conformance on the 15% Peer-Review-Remediation dimension.
- (event by user) Post-audit close-out: user reviewed the clause-by-clause audit against the full Spec-2 task statement. Item #1 (consolidated 4-column traceability matrix) applied. Items #2 (keep [ILLUSTRATIVE DEFAULT] number-avoidance as-is, do not add literal KPI targets), #3 (do not renumber CAPs to the task's example numbering), #4 (no extra prompt-transcript detail in memlog) DISCARDED by user decision. Bundle is final and submission-ready; nothing committed - commit is the user's call.
- (event) Mermaid diagrams RE-VERIFIED at close-out (fresh environment): rendered both companion-files/diagrams/*.mmd with mermaid-cli mmdc + Chrome 152.0.7977.42 (--no-sandbox --single-process, executablePath pinned to cached puppeteer chrome). system-context.mmd -> valid flowchart SVG 74765 bytes, 58 foreignObject labels, 0 error elements. state-transitions.mmd -> valid stateDiagram SVG 84751 bytes, 51 labels, 0 error elements. Only 'error' string in either SVG is Mermaid's boilerplate .error-icon/.error-text CSS class defs (present in every Mermaid SVG). No 'Syntax error' text, no error fallback DOM. Both confirmed rendering clean.
